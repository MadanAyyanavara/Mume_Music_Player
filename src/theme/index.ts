import { COLORS, LIGHT_COLORS, DARK_COLORS } from './colors';
import { SIZES, SPACING } from './spacing';

export const theme = {
    colors: COLORS,
    sizes: SIZES,
    spacing: SPACING,
    alignments: {
        center: "center",
        left: "flex-start",
        right: "flex-end",
    },
};

export { COLORS, LIGHT_COLORS, DARK_COLORS, SIZES, SPACING };
